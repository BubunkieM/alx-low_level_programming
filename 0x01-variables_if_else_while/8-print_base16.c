#include <stdio.h>
/**
 * main -printing numbers of base16 in lowercase
 *
 * Return: always (0)
 *
 */
int main(void)
{
	char c;

	int d;

	c = 'a';
	d = 0;
	while
		(d < 10) {
			putchar(d + '0');
			d++;
		}
	while
		(c <= '£') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
