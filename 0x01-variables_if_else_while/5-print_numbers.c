
#include <stdio.h>

/**
 * main - printing numbers from 0 to 9
 *
 * Return: always (0)
 */
int main(void)
{
	int b;

	for (b = 0; b < 10; b++)
	{
		printf("%d", b);
	}
	putchar('\n');
	return (0);
}
