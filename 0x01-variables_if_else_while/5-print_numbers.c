#include <stdio.h>
/**
 * main -printing single digits number base10 starting from 0
 *
 * Return: always (0)
 *
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}

