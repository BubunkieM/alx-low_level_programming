#include <stdio.h>
/**
 * main -single digit numberz on base10 starting from 0
 *
 * Return: always (0)
 *
 */
int main(void)
{
	int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + '0');
			c++;
		}
	putchar('\n');
	return (0);
}
